# usage
When you first download the program, or every time you re-download `quotes.txt`,
you will wnat to run ```python3 update_indices.py```.  
Generally, run with ```python3 get_quotes.py``` or put that in your `.bashrc` file.
